name: Smoke Test

on:
  push:
    # 与 ci.yml 同理：dev 无保护，PR 的 pull_request run 已覆盖同一 commit。
    branches: [main]
  pull_request:
    branches: [main, dev]

jobs:
  smoke:
    name: Smoke Test (${{ matrix.platform }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        platform: [github, gitlab, gitcode]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Build
        run: cargo build --release

      - name: Add to PATH
        run: echo "${{ github.workspace }}/target/release" >> $GITHUB_PATH

      - name: Verify installation
        run: |
          gf --version
          gf --help

      - name: Run smoke test (read-only)
        run: bash scripts/smoke-test.sh --platform ${{ matrix.platform }} --read-only

      - name: Run smoke test (write mode help only)
        run: bash scripts/smoke-test.sh --platform ${{ matrix.platform }} --write
        if: success()
