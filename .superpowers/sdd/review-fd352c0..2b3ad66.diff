# Review package: fd352c0..HEAD

## Commits
2b3ad66 docs: analyze gitflow-weekly-report skill (#23)

## Files changed
 .../skill-analysis-gitflow-weekly-report.md        | 297 +++++++++++++++++++++
 1 file changed, 297 insertions(+)

## Diff
diff --git a/docs/research/skill-analysis-gitflow-weekly-report.md b/docs/research/skill-analysis-gitflow-weekly-report.md
new file mode 100644
index 0000000..867fe40
--- /dev/null
+++ b/docs/research/skill-analysis-gitflow-weekly-report.md
@@ -0,0 +1,297 @@
+# gitflow-weekly-report Skill 分析报告
+
+> **分析日期：** 2026-07-07
+> **分析目标：** `skills/gitflow-weekly-report/SKILL.md`
+> **对应 Issue：** #23
+> **分析维度：** 4 个维度（结构规范、职责边界、可测试性、Superpowers 最佳实践）
+
+---
+
+## 一、当前状态总览
+
+| 维度 | 评分 | 说明 |
+|------|------|------|
+| 维度 1：Skill 结构和文档规范 | ⚠️ 需改进 | frontmatter 存在但 description 混合功能描述与触发关键词；无 Overview / When to Use / Quick Reference 章节；token 偏高 (~700-900 词当量)；输出模板占大量篇幅 |
+| 维度 2：职责边界清晰度 | ❌ 不合格 | 完全缺失职责边界声明、禁止行为清单和红旗列表；跨项目读取尤其需要边界声明 |
+| 维度 3：可测试性 | ❌ 不合格 | 完全缺失测试场景、基线测试和成功标准 |
+| 维度 4：与 Superpowers 最佳实践的差距 | ⚠️ 需改进 | 触发关键词存在但嵌入功能描述中；无跨引用；未遵循 TDD；分类逻辑和模板质量较高 |
+
+**总体评估：** gitflow-weekly-report 当前是一份"报告生成模板 + 分类指南"，在输出模板和分类逻辑上做得相当扎实（6 维度分类、明确模板、具体场景解析），但不符合 Superpowers 对 skill 的结构性要求。它告诉 Claude "输出什么样的报告" 和 "如何分类"，但没有回答"何时触发此 skill""跨项目读取时边界在哪""何时不应推断/虚构数据""如何验证报告质量"。
+
+---
+
+## 二、维度 1 分析 — Skill 结构和文档规范
+
+### 2.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| YAML frontmatter 含 name 字段 | ✅ | `name: gitflow-weekly-report` |
+| YAML frontmatter 含 description 字段 | ✅ | 存在 description |
+| description 以 "Use when..." 开头 | ❌ | 当前为 "生成研发周报…" 功能描述，未使用触发条件格式 |
+| description 只描述触发条件 | ❌ | description 混合功能描述 + "TRIGGER when" 关键词，二者未分离 |
+| 含 Overview 章节 | ⚠️ | 有简短介绍段落，但非正式 Overview 章节 |
+| 含 When to Use 章节 | ❌ | 无 |
+| 含 Core Pattern 章节 | ❌ | 无 |
+| 含 Quick Reference | ❌ | 无"触发 → 执行 → 输出" 快速对照 |
+| 含 Implementation 章节 | ⚠️ | 有 4 步骤工作流，但偏向描述性说明而非命令式指令 |
+| 含 Common Mistakes | ❌ | 无结构化常见错误说明 |
+| Token 效率 < 500 词 | ❌ | 估算约 700-900 词当量（含 40 行输出模板），远超建议上限 |
+| 无叙事性示例反模式 | ✅ | 常用场景描述为抽象示例（无虚构项目名称/数据），输出模板使用占位符 |
+| 无多语言稀释 | ⚠️ | description 夹杂中文功能描述 + 英文 "TRIGGER when:" 前缀关键词，格式不统一 |
+| 无流程图中嵌入代码 | ✅ | 无流程图 |
+
+### 2.2 具体问题
+
+1. **Frontmatter description 违反 Superpowers 规范**：
+   - 当前：`生成研发周报，扫描多个 Git 仓库的提交记录…跨项目工具，推荐用户级安装。TRIGGER when: 用户要生成周报…`
+   - 问题：description 字段混合了三类内容——功能说明、安装建议、触发关键词
+   - 应为：`Use when the user asks to generate a weekly report, summarize recent commits, review weekly work output, or says "周报", "weekly report", "本周工作", "上周做了什么", "多项目周报".`
+   - 安装建议（`gitflow-cli skills install -g`）不应放在 description 中，应放在 Overview 或独立的 Installation 章节
+   - TRIGGER 关键词（周报、本周工作总结、weekly report 等）应作为独立的触发条件列表，而非嵌入 description
+
+2. **文档结构偏向"模板展示"而非"可执行指令"**：
+   - 当前结构：输入说明 → 4 步骤工作流 → 分类汇总 → 输出模板 + 报告规范 → 常见场景 → 边界处理 → 注意事项
+   - 输出模板占据文档近 40% 篇幅（含完整报告结构），作为"模板"合理但大幅增加 token 消耗
+   - 缺少 Superpowers 要求的核心章节：
+     - 前置条件（是否需要特定工具？是否需要多仓库访问权限？）
+     - 输入验证（项目路径不存在时的完整处理）
+     - 条件分支（提交数 = 0 → 是否仍生成完整模板？）
+     - 输出汇总（"用户最终收到什么"一句话摘要）
+
+3. **Token 效率问题**：
+   - 当前约 700-900 词当量，远超 500 词上限
+   - 主要贡献者：
+     - 完整输出模板 (~30 行 × 多种占位符)
+     - 3 个常见场景解析（含解析逻辑）
+     - 6 维度分类说明
+   - 优化方向：将输出模板移至独立模板文件（如 `templates/weekly-report.tmpl`），skill 中仅保留模板引用和关键约束
+
+4. **Implementation 步骤偏描述性，缺少命令式骨架**：
+   - 当前步骤 1-4 以"做什么"为主，缺少"如何做"的精确指令
+   - 例如步骤 1 说"解析 `--since` 和 `--until`"，但没有给出 ISO week 计算的确定性逻辑
+   - 步骤 2 包含 bash 命令示例（好），但它们是"示例"还是"必须执行的命令"不明确
+   - 步骤 3 的分类规则可用关键词匹配实现，但没有给出决策流程
+
+### 2.3 评分：⚠️ 需改进
+
+---
+
+## 三、维度 2 分析 — 职责边界清晰度
+
+### 3.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| 有明确的职责边界声明 | ❌ | 完全缺失 |
+| 有禁止行为清单（🚫 不得...） | ❌ | 缺失 |
+| 有职责范围说明（✅ 负责... / ❌ 不负责...） | ❌ | 缺失 |
+| 有"合理化借口"反制表格 | ❌ | 缺失 |
+| 有红旗列表（Red Flags） | ❌ | 缺失 |
+
+### 3.2 具体问题
+
+1. **完全无职责边界 — 跨项目读取 skill 尤其需要边界声明**：
+   - 本 skill 可扫描多个仓库路径（用户提供任意本地路径），无边界声明可能导致：
+     - Claude 尝试扫描用户列出的敏感目录（如包含密钥或专有代码的公司内部仓库）
+     - Claude 读取 `.git` 之外的文件来"补充周报内容"
+     - Claude 为增加周报"深度"而推断用户未提交的 WIP 工作内容
+     - Claude 基于提交模式评估开发者绩效（"提交偏少" → 负面评价）
+   - 职责边界风险等级：🟡 中-高（跨项目读取 + 生成评价性文本）
+
+2. **"未完成事项"推断与"不可虚构数据"规则存在张力**：
+   - 步骤 5 规则：`'未完成事项"要结合上下文推断——如果某个项目提交明显偏少或明显有未收尾的工作，应在此处体现'`
+   - 注意事项第 4 条：`提交数、日期等数据必须从 git 实际获取，不可编造`
+   - 张力：推断"未收尾工作"本质上是基于提交频率的主观判断 + 潜在虚构，与"不可编造"原则冲突
+   - 需要明确边界：哪些字段可从 git log 直接提取（提交数、日期、hash、message），哪些需要推断（"是否收尾"），推断的确定性阈值是多少
+
+3. **缺少禁止行为清单** — 应明确：
+   - 🚫 不得读取 `.git` 之外的文件来"补充"周报内容
+   - 🚫 不得访问未在用户指定路径列表中的仓库
+   - 🚫 不得对开发者的提交频率、代码质量做负面绩效评价
+   - 🚫 不得在报告中包含未从 git 获取的虚构数据（如估算提交数、虚构完成事项）
+   - 🚫 不得修改仓库（不执行 git commit / push / rebase 等写操作）
+   - 🚫 不得将周报内容发送到仓库外部的服务
+
+4. **缺少红旗信号** — 应标识：
+   - 用户提供的路径包含多个无关仓库（可能无意暴露敏感项目）
+   - 用户要求"评估某人工作表现"或"比较团队产出"
+   - 用户要求扫描不在指定列表中的目录（"扫描这整个主目录"）
+   - 扫描结果可能暴露安全相关信息（如某仓库的提交模式暗示特定部署时间）
+
+5. **缺少职责范围说明**：
+   - ✅ 负责：读取指定仓库的 git log、分类提交、按模板格式输出周报
+   - ❌ 不负责：访问仓库外文件、评估开发者绩效、自动创建 Issue/PR/Commit、分析代码质量、生成建议的"下周工作"是否准确可靠
+
+### 3.3 评分：❌ 不合格
+
+---
+
+## 四、维度 3 分析 — 可测试性
+
+### 4.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| 定义了测试场景 | ❌ | 无测试场景定义 |
+| 有基线测试（无 skill 时的行为） | ❌ | 缺失 |
+| 有压力测试场景 | ❌ | 缺失 |
+| 有成功标准 | ❌ | 缺失 |
+| 可使用 writing-skills 方法论测试 | ❌ | 无测试钩子 |
+
+### 4.2 具体问题
+
+1. **零测试覆盖 — 无法验证 Claude 执行质量**：
+   - 如何判断"Claude 对提交的分类准确"？（`feat:` 前缀 vs 实际功能描述分类是否一致？）
+   - 如何判断"Claude 在多项目场景下正确分组"？
+   - 如何判断"Claude 没有编造'下周工作建议'"？
+   - 如何判断"Claude 正确处理了跨年的周计算"？
+
+2. **缺少基线对比**：
+   - 不使用 skill 时，Claude 的典型行为是：直接运行 `git log --oneline`，输出原始 commit 列表，按日期简单分组
+   - 差距正是 skill 价值：分类合并、模板格式化、提交统计
+   - 基线行为未明确记录，无法定义 skill 的增量价值
+
+3. **无压力测试场景**：
+   - 单个项目 >1000 个提交的极繁忙周（摘要合并的颗粒度）
+   - 多个项目在不同时区（截止时间的解释）
+   - commit message 完全不符合 Conventional Commits 格式的项目（分类失败）
+   - 项目路径包含空格或特殊字符（shell 安全性）
+
+4. **无成功标准**：
+   - 应定义：报告必须包含哪些章节（输入说明中的 4 类章节）
+   - 应定义：分类覆盖率阈值（应有 X% 的提交被分类，不得全部归入 "其他"）
+   - 应定义：提交数统计必须与 `git log | wc -l` 结果完全匹配
+   - 应定义：所有 commit hash 必须真实存在于仓库中
+
+### 4.3 评分：❌ 不合格
+
+---
+
+## 五、维度 4 分析 — 与 Superpowers 最佳实践的差距
+
+### 5.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| 遵循 TDD for skills（RED-GREEN-REFACTOR） | ❌ | 无 TDD 流程记录 |
+| description 只描述触发条件，不描述流程 | ❌ | description 混合功能描述、安装建议和触发关键词 |
+| 关键词覆盖（错误信息、症状、同义词、工具） | ⚠️ | 有中文触发词但嵌入 description；未覆盖英文变体（"weekly summary", "commit recap"） |
+| 跨引用其他 skills | ❌ | 无 See Also / 相关 Skills |
+| 必要时使用 flowchart | N/A | 工作流是线性步骤 + 分类判断，无需流程图 |
+
+### 5.2 具体问题
+
+1. **description 格式不符合 Superpowers 规范**：
+   - ❌ 当前：多行中文功能描述 + 英文 "TRIGGER when" 标签 + 触发关键词混合
+   - ✅ 应为：`Use when the user asks to generate a weekly work report, summarize recent commits, review weekly output, or says "周报", "weekly report", "本周工作", "上周做了什么", "commit recap", "多项目周报".`
+
+2. **关键词覆盖不完整**：
+   - 当前有：周报、weekly report、本周工作总结、上周做了什么、生成研发周报、多项目周报
+   - 缺失英文触发词：`weekly summary`、`commit recap`、`week in review`、`sprint summary`、`work digest`
+   - 缺失同义词：`提交汇总`、`工作量统计`、`本周 commit`、`项目周报`
+   - 缺失场景触发："这周干了啥"、"帮我整理一下"、"review the week"
+
+3. **缺少跨引用**：
+   - 应引用 `gitflow-commit`（commit message 规范影响分类准确性）
+   - 应引用 `gitflow-repo`（仓库管理和访问）
+   - 应引用 `gitflow-auth`（跨仓库场景下的权限问题）
+   - 应引用 `superpowers:verification-before-completion`（周报数据需要验证，提交数不可编造）
+
+4. **内容组织偏向"模板 + 规则说明"而非"执行指令"**：
+   - Superpowers skill 应告诉 Claude "做什么 + 怎么做 + 不做什么"
+   - 当前文档告诉读者（人）"输出应该长什么样"和"分类应该怎么做"
+   - 缺少"条件 → 动作"决策逻辑（如果 X 则 Y，否则 Z）
+
+5. **未遵循 writing-skills 方法论**：
+   - 当前 skill 似乎是"基于模板一次性编写"，未记录 baseline → 编写 → 验证 → 迭代过程
+   - "常见场景"部分有场景意识，但缺少对应的"预期输出"对比
+
+### 5.3 评分：⚠️ 需改进
+
+---
+
+## 六、改进建议
+
+### P0（必须修复 — 阻断性问题）
+
+| # | 改进项 | 维度 | 说明 |
+|---|--------|------|------|
+| P0-1 | 重写 description 为纯触发条件 | D1, D4 | description 只含触发条件，使用 "Use when..." 格式，移除功能描述、安装建议和 TRIGGER when 标签 |
+| P0-2 | 添加职责边界声明章节 | D2 | 明确扫描范围（仅限 git log）、禁止读取仓库外文件、禁止绩效评价 |
+| P0-3 | 添加禁止行为清单 | D2 | 🚫 不得读取 .git 外文件、🚫 不得编造提交数/日期、🚫 不得推断负面绩效评价、🚫 不得修改仓库 |
+| P0-4 | 添加红旗列表 | D2 | 标识：用户要求评价他人绩效、路径包含大量无关仓库、提交数要求"估算" |
+| P0-5 | 分离"不可虚构数据"与"推断未收尾工作"的规则 | D1, D2 | 明确"未完成事项"章节中可以写什么（仅基于 commit message 中明显的 WIP 标记）、不可以写什么（基于提交频率的主观推断） |
+| P0-6 | 添加基线测试场景 | D3 | 定义不用 skill 时 Claude 的基线行为（直接 `git log --oneline`），作为 skill 增量价值的锚点 |
+
+### P1（建议修复 — 提升质量）
+
+| # | 改进项 | 维度 | 说明 |
+|---|--------|------|------|
+| P1-1 | 重构为结构化模板 | D1 | 添加 Overview / When to Use / Core Pattern / Quick Reference / Implementation / Common Mistakes 章节 |
+| P1-2 | 提取输出模板至独立文件 | D1 | 将完整报告模板移至 `templates/weekly-report.tmpl`，skill 中引用路径，降低 token 占用至 < 500 词 |
+| P1-3 | 添加前置条件检查 | D1 | 执行前验证：路径是否存在、是否为 git 仓库、是否有读取权限 |
+| P1-4 | 添加决策逻辑（条件分支） | D1, D4 | 提交数 = 0 → 仍生成模板但注明"本周暂无提交"；路径不存在 → 跳过并警告 |
+| P1-5 | 添加跨引用 | D4 | 引用 gitflow-commit、gitflow-repo、gitflow-auth、superpowers:verification-before-completion |
+| P1-6 | 补充关键词覆盖 | D4 | 添加英文触发词（weekly summary、commit recap、sprint summary）和同义中文词（提交汇总、工作量统计） |
+| P1-7 | 定义成功标准 | D3 | 报告章节完整性、分类覆盖率、提交数与 `git log | wc -l` 一致性、hash 真实性 |
+
+### P2（可选改进 — 锦上添花）
+
+| # | 改进项 | 维度 | 说明 |
+|---|--------|------|------|
+| P2-1 | 定义测试场景和预期输出 | D3 | 3-5 个标准输入（单项目、多项目、空提交、跨年、混合语言 commit message），附预期报告片段 |
+| P2-2 | 添加压力测试场景 | D3 | 超大型项目（>5000 提交）、路径含特殊字符、全为非规范 commit message |
+| P2-3 | 添加错误处理章节 | D1 | 覆盖：路径不存在、非 git 仓库、权限不足、无提交、含特殊字符的路径 |
+| P2-4 | 提供英文版 description | D1 | description 用英文 + 中文触发词混合，更符合 Superpowers 主流 |
+| P2-5 | 添加 TDD for skills 验证记录 | D3, D4 | 记录 baseline → 编写 → 测试 → 迭代的完整过程 |
+| P2-6 | 分类规则参考 gitflow-commit | D1, D4 | 将 Conventional Commits 类型映射直接关联，避免两套规范冲突 |
+
+---
+
+## 七、验收标准
+
+- [ ] description 以 "Use when..." 开头，仅描述触发条件（英文为主 + 中文关键词）
+- [ ] description 不含功能描述、安装建议或 "TRIGGER when" 标签
+- [ ] 含职责边界声明章节（含 🚫 禁止行为、✅ 职责范围）
+- [ ] 含红旗列表（绩效评价请求、大量无关路径、编造数据要求等）
+- [ ] "未完成事项"推断规则与"不可虚构数据"原则无张力
+- [ ] 含关键词覆盖（中英文触发词、同义词）
+- [ ] 含跨引用（至少引用 2 个相关 skill）
+- [ ] 文档结构包含 Overview / When to Use / Core Pattern / Quick Reference / Implementation
+- [ ] 前置条件检查和错误处理章节
+- [ ] Token 数 < 500 词（输出模板移至独立文件）
+- [ ] 定义至少 3 个基线测试场景和成功标准
+- [ ] 分类规则与 gitflow-commit 的 Conventional Commits 规范一致
+
+---
+
+## 八、与同类 Skill 对比
+
+| 对比项 | gitflow-weekly-report | gitflow-autoreport-bug | gitflow-security-check |
+|--------|----------------------|----------------------|----------------------|
+| 职责边界 | ❌ 缺失 | ✅ 完整 | ❌ 缺失 |
+| 测试场景 | ❌ 缺失 | ❌ 缺失 | ❌ 缺失 |
+| 触发条件 | ⚠️ 混合格式 | ⚠️ 描述流程 | ❌ 功能描述 |
+| 跨引用 | ❌ 缺失 | ❌ 缺失 | ❌ 缺失 |
+| 分类逻辑质量 | ✅ 高（6 维度） | N/A | N/A |
+| 输出模板质量 | ✅ 高（完整结构） | N/A | ⚠️ 含虚构数据 |
+| 职责边界风险等级 | 🟡 中-高（跨项目读取） | 🟡 中（Issue 创建） | 🔴 高（敏感数据） |
+| Token 估算 | ❌ 700-900 词（超标） | ⚠️ 中 | ⚠️ 接近上限 |
+
+**关键发现：** gitflow-weekly-report 在"输出模板 + 分类逻辑"维度上项目最佳（6 维度分类、3 个常见场景、完整模板），但其跨项目读取能力和数据推断需求使其职责边界风险高于普通的单项目 skill。高内容质量 + 中等边界风险 = 需要明确"推断"与"编造"的边界。
+
+---
+
+## 九、总结
+
+gitflow-weekly-report 当前的定位是"报告生成模板 + 分类规则指南"，它在**输出结构的完整性**（含统计、未完成事项、需要协调的问题、下周工作建议四层结构）和**分类逻辑的细致度**（6 维度分类 + 同方向合并原则）上表现优秀，在**常见场景覆盖**（多项目、指定截止时间、包含周日）上也有周全考虑。
+
+核心差距：
+1. **触发条件与功能描述混合** → Claude 何时应加载此 skill？当前 description 承载了过多职责
+2. **缺乏跨项目读取的边界声明** → Claude 应访问哪些数据源？是否可以用"推测"填补模板空白？
+3. **"未收尾工作推断"与"不可虚构数据"原则张力** → 需要明确哪些推断是被允许的
+4. **Token 超标** → 输出模板占近 40% 篇幅，可提取至独立文件
+5. **缺乏可测试性设计** → 无法验证 Claude 的报告质量是否达标
+
+重构方向：保持分类逻辑和场景覆盖的质量优势（可移至 `docs/weekly-report-guide.md`），将 skill 重构为"精简触发条件 + 执行骨架 + 职责边界 + 条件分支 + 模板引用 + 成功标准"的可执行指令。将输出模板和详细分类规则移至独立模板文件，使 skill token 降至 < 500 词。
