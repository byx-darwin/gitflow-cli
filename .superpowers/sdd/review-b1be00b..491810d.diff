# Review package: b1be00b..HEAD

## Commits
491810d docs: analyze gitflow-regression skill (#25)

## Files changed
 docs/research/skill-analysis-gitflow-regression.md | 309 +++++++++++++++++++++
 1 file changed, 309 insertions(+)

## Diff
diff --git a/docs/research/skill-analysis-gitflow-regression.md b/docs/research/skill-analysis-gitflow-regression.md
new file mode 100644
index 0000000..3181938
--- /dev/null
+++ b/docs/research/skill-analysis-gitflow-regression.md
@@ -0,0 +1,309 @@
+# gitflow-regression Skill 分析报告
+
+> **分析日期：** 2026-07-07
+> **分析目标：** `skills/gitflow-regression/SKILL.md`
+> **对应 Issue：** #25
+> **分析维度：** 4 个维度（结构规范、职责边界、可测试性、Superpowers 最佳实践）
+
+---
+
+## 一、当前状态总览
+
+| 维度 | 评分 | 说明 |
+|------|------|------|
+| 维度 1：Skill 结构和文档规范 | ⚠️ 需改进 | description 混合功能描述+流程描述，非 "Use when..." 格式；缺少 Overview / When to Use / Core Pattern / Quick Reference 章节；使用示例和操作步骤超长（885 词）；前置检查嵌入大段 shell 脚本 |
+| 维度 2：职责边界清晰度 | ❌ 不合格 | 完全缺失职责边界声明、禁止行为清单、红旗列表和"合理化借口"反制表格；且 skill 涉及跨平台操作和自动创建 Issue（副作用） |
+| 维度 3：可测试性 | ❌ 不合格 | 完全缺失测试场景、基线测试、压力测试和成功标准 |
+| 维度 4：与 Superpowers 最佳实践的差距 | ⚠️ 需改进 | 工作流步骤清晰、命令实用；但 description 偏离规范、无关键词覆盖、无跨引用、未遵循 writing-skills 方法论 |
+
+**总体评估：** gitflow-regression 是一个"运行脚本 + 解析输出 + 上报失败"的实用型 skill。技术内容质量高（平台选项、失败分类、自动上报逻辑都有详细覆盖）。但与 Superpowers skill 形态的差距在于：**description 不合规导致触发边界模糊**，**职责边界完全缺失**（涉及跨平台副作用以及通过 autoreport-bug 间接创建 Issue），**无可测试性设计**。
+
+---
+
+## 二、维度 1 分析 — Skill 结构和文档规范
+
+### 2.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| YAML frontmatter 含 name 字段 | ✅ | `name: gitflow-regression` |
+| YAML frontmatter 含 description 字段 | ✅ | 存在 description |
+| description 以 "Use when..." 开头 | ❌ | "冒烟测试工作流 — 运行 scripts/smoke-test.sh..." |
+| description 只描述触发条件 | ❌ | 混合工作流描述（"运行...解析...失败时自动调用..."）和流程承诺 |
+| 含 Overview 章节 | ⚠️ | 有 H1 标题和功能介绍，但非结构化 Overview |
+| 含 When to Use 章节 | ❌ | 无触发条件说明 |
+| 含 Core Pattern 章节 | ❌ | 无核心模式/算法骨架 |
+| 含 Quick Reference | ❌ | 5 个工作流步骤（步骤 1-5）缺少"触发→执行→输出"速查 |
+| 含 Implementation 章节 | ✅ | 步骤 1-5 工作流明确可执行（高质量） |
+| 含 Common Mistakes | ⚠️ | 有"注意事项"段落（9 条），接近 Common Mistakes |
+| Token 效率 < 500 词 | ❌ | 885 词（`wc -w`），超标约 385 词（77%） |
+| 无叙事性示例反模式 | ⚠️ | 使用示例章节给出 5 个完整 bash 块，接近"复制黏贴脚本模板"而非"指导执行" |
+| 无多语言稀释 | ⚠️ | 全中文，未提供英文 description |
+| 无流程图中嵌入代码 | ✅ | 无 Mermaid/AST 流程图嵌入 |
+
+### 2.2 具体问题
+
+1. **description 违反 Superpowers 规范**：
+   - 当前：`冒烟测试工作流 — 运行 scripts/smoke-test.sh 执行端到端冒烟测试，解析测试结果，失败时自动调用 gitflow-autoreport-bug 上报`
+   - 问题：这是功能描述 + 工作流承诺，不是触发条件
+   - 应为：`Use when the user runs smoke/regression tests against the gitflow CLI, needs to parse test results to find regressions, or wants automatic bug reporting for smoke-test failures.`
+   - 后果：用户说"跑一下回归测试"、"smoke test"、"看看上次改动有没有破坏功能"都是潜在触发，但当前 description 无法帮助 Claude 判断
+
+2. **Token 超标（885 词 vs 500 上限）**：
+   - 主因：
+     - 前置检查嵌入大段 shell 脚本（约 20 行，含 4 个条件分支）
+     - 使用示例给出 5 个完整场景（约 60 行，含多个 bash heredoc）
+     - 步骤 4 详细说明失败上报流程（约 50 行，含分类表和 JSON 模板）
+   - 问题：skill 本质是"调用脚本 + 解析输出"的简单流程，但文档膨胀成完整操作手册
+   - 建议：前置检查浓缩为 3 行命令速查；使用示例保留 1 个最简示例；失败分类表移至 `docs/research/`
+
+3. **缺少结构化快速导航**：
+   - Superpowers skill 推荐 Overview + When to Use + Core Pattern + Quick Reference + Implementation + Common Mistakes
+   - 当前 skill 给出 5 步骤线性工作流，用户无法快速判断"这个 skill 做什么、何时用、有何风险"
+   - 建议：
+     - 添加 Overview 段落（1-2 句概括能力）
+     - 添加 When to Use 触发关键词
+     - 添加 Quick Reference 速查：`bash scripts/smoke-test.sh --platform <platform> [--verbose] [--write]`
+
+4. **使用示例偏重"展示"而非"指导"**：
+   - "运行基本冒烟测试"、"运行 GitLab 平台的冒烟测试"、"运行带写入操作的冒烟测试"、"集成到 CI 流程"、"失败后自动上报"——5 个示例全是完整 bash 块
+   - 真实触发场景通常是"跑一下 smoke test"或"上次改动后回归正常吗"
+   - 建议：1 个核心示例 + 3 行变体速查
+
+5. **步骤 4 的失败上报逻辑过于详尽**：
+   - 详细描述 `pending.json` 结构、md5 求和、目录创建、JSON 写入——这些是 `gitflow-autoreport-bug` skill 自己的职责
+   - 跨 skill 冗余：在 regression skill 中完整描述 autoreport-bug 的内部 JSON 格式违反 DRY
+   - 建议：upstream 直接引用 `gitflow-autoreport-bug` skill，不重述实现细节
+
+### 2.3 评分：⚠️ 需改进
+
+---
+
+## 三、维度 2 分析 — 职责边界清晰度
+
+### 3.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| 有明确的职责边界声明 | ❌ | 完全缺失 |
+| 有禁止行为清单（🚫 不得...） | ❌ | 缺失 |
+| 有职责范围说明（✅ 负责... / ❌ 不负责...） | ❌ | 缺失 |
+| 有"合理化借口"反制表格 | ❌ | 缺失 |
+| 有红旗列表（Red Flags） | ❌ | 缺失 |
+
+### 3.2 具体问题
+
+1. **无职责边界 → 涉及跨平台副作用与间接 Issue 创建**：
+   - 本 skill 会调用 `gitflow-autoreport-bug`，间接导致 GitHub/GitLab/GitCode 上创建 Issue
+   - 写入模式（`--write`）可能实际创建/修改远程资源
+   - 没有边界声明时，Claude 可能在以下场景过度执行：
+     - 用户仅询问"上次 CI 失败了吗"时，Claude 自动运行写模式 smoke test 并在远端创建 Issue
+     - 认证失效导致的失败也被当作真实 bug 上报（浪费 Issue编号）
+     - 警告用户环境无认证凭据 → 直接运行 smoke test → 认证失败 → 自动创建多个 bug Issue
+
+2. **缺少禁止行为清单 — 应明确**：
+   - 🚫 不得在用户未明确要求时运行写入模式（`--write`）——会创建远端资源
+   - 🚫 不得将认证/网络问题引起的失败自动分类为 CLI bug 上报
+   - 🚫 不得在 CI 环境中间接调用 autoreport-bug（双重日志冗余）
+   - 🚫 不得抑制临时性错误（网络抖动、429 Too Many Requests）
+   - 🚫 不得在未确认用户环境有有效认证凭据前运行测试
+
+3. **缺少红旗信号 — 应标识**：
+   - 用户说"先不管认证问题，看看功能正常吗"
+   - 用户说"跑写模式测试"却不在受控环境
+   - 测试环境不是 GitHub/GitLab/GitCode 之一
+   - 用户说"临时跑一下，不报 Issue"
+   - 上一次运行已触发 autoreport-bug，但 Issue 尚未被关闭（重复上报）
+
+4. **缺少职责范围说明**：
+   - ✅ 负责：调用 smoke-test.sh 脚本、解析 PASS/FAIL/SKIP 结果、分类失败类型、生成测试报告
+   - ❌ 不负责：修复 bug（通过 autoreport-bug 间接上报，不直接修复代码）、配置远端环境、修改 scripts/smoke-test.sh
+   - ❌ 不负责：确保 Issue 关闭（autoreport-bug 只创建）
+
+5. **"合理化借口"反制**：
+   - "it's just a smoke test" → "只是个冒烟测试，不会有问题" → 写入模式也应确认
+   - "auto-fix the flakes" → "跳过失败项继续跑" → flaky 失败仍应上报或显式豁免
+   - "I'll check auth later" → "以后再说" → 认证缺失时应拒绝运行或仅解析脚本 --help
+
+### 3.3 评分：❌ 不合格
+
+**对比参考（gitflow-autoreport-bug）：** autoreport-bug 本身有完整的"职责边界声明"章节。但 regression skill 调用 autoreport-bug 形成间接副作用链——regression 的"边界"不仅限于自身操作，还应约束下游调用。这是更复杂的链式边界场景，比 autoreport-bug 单 skill 更需要声明。
+
+---
+
+## 四、维度 3 分析 — 可测试性
+
+### 4.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| 定义了测试场景 | ❌ | 无测试场景定义 |
+| 有基线测试（无 skill 时的行为） | ❌ | 缺失 |
+| 有压力测试场景 | ❌ | 缺失 |
+| 有成功标准 | ❌ | 仅有格式化"结论"输出，未定义为可验证标准 |
+| 可使用 writing-skills 方法论测试 | ❌ | 无测试钩子 |
+
+### 4.2 具体问题
+
+1. **零测试覆盖 — 无法验证 Claude 执行质量**：
+   - 如何判断 "Claude 正确 --read-only 模式作为默认"？
+   - 如何判断 "Claude 在认证缺失时不调用 smoke-test.sh（避免无意义的失败上报）"？
+   - 如何判断 "Claude 对 `command not found` 类失败正确识别为'环境问题而非 CLI bug'"并抑制上报？
+   - 如何判断 "Claude 在失败类型分类上使用正确严重级别"？
+
+2. **缺少基线对比**：
+   - 基线行为：用户说"run smoke tests just to see" → Claude 可能仅运行 `bash scripts/smoke-test.sh`，不捕获退出码、不解析输出、不生成结构化报告
+   - 基线行为：用户说"spoxe test"（typo）→ Claude 可能首先找不到命令或误判为 typo 建议 `spoxe` 替代词
+   - 基线行为：用户说"any regressions?" → Claude 可能仅回答猜测，不主动运行 smoke-test.sh
+
+3. **无压力测试场景**：
+   - smoke-test.sh 所有测试都失败（CLI 已完全损坏）→ autoreport-bug 会为每个失败单独生成 Issue → 大量重复
+   - 所有测试都被跳过（e.g. 网络隔离 + write mode）→ 应报告"无法运行"而非全部失败
+   - `scripts/smoke-test.sh` 文件本身损坏或缺失 → 当前 skill 提到前置检查但未描述失败后的"优雅退出"路径
+   - 用户使用不存在的平台名 `--platform fake`：skill 未说明如何验证 `platform` 参数有效性
+
+4. **无成功标准**：
+   - 应定义：成功跑完 read-only 报告的最低字段（PASS 数、FAIL 数、SKIP 数、结论一句话）
+   - 应定义：失败上报前应验证什么？（认证网络可达、FAIL_COUNT > 0、失败项确实非临时性）
+   - 应定义：写入模式成功标志——测试完后不应遗留新创建的远端测试资源
+
+5. **未使用 writing-skills 方法论验证**：
+   - skill 未经历 baseline → gap analysis → write skill → verify 的 TDD 循环
+   - skill 创建时未记录"不用 skill 时的 Claude 差距是什么"
+
+### 4.3 评分：❌ 不合格
+
+---
+
+## 五、维度 4 分析 — 与 Superpowers 最佳实践的差距
+
+### 5.1 检查清单
+
+| 检查项 | 状态 | 说明 |
+|--------|------|------|
+| 遵循 TDD for skills（RED-GREEN-REFACTOR） | ❌ | 无 TDD 流程记录 |
+| description 只描述触发条件，不描述流程 | ❌ | description 混合功能承诺和工作流描述 |
+| 关键词覆盖（错误信息、症状、同义词、工具） | ⚠️ | 提到 `smoke-test.sh`、`PASS/FAIL/SKICK`、`read-only`、`write`、`--platform`、`gitflow-autoreport-bug` 等工具/文件和选项 |
+| 跨引用其他 skills | ❌ | 仅文字提及调用 `gitflow-autoreport-bug`，无结构化 See Also / 相关 Skills |
+| 必要时使用 flowchart | ⚠️ | 当前无流程图，但 5 步骤工作流 + 2 个决策点（认证检查、写入模式确认）其实需要简化流程图指导 |
+
+### 5.2 具体问题
+
+1. **description 应为触发条件，当前是功能描述**：
+   - ❌ 当前：`冒烟测试工作流 — 运行 scripts/smoke-test.sh 执行端到端冒烟测试，解析测试结果，失败时自动调用 gitflow-autoreport-bug 上报`
+   - ✅ 应为：`Use when the user runs smoke/regression tests against the gitflow CLI, needs to parse test results for regressions, or wants automatic bug reporting for smoke-test failures.`
+   - 关键词覆盖建议（中文）："跑回归测试"、"smoke test"、"回归"、"冒烟"、"验证 CLI 功能"、"跑 smoke"、"test against all platforms"
+   - 关键词覆盖建议（英文）："smoke test"、"regression test"、"regression check"、"end-to-end check"、"verify gitflow works"、"pre-release check"
+
+2. **缺少结构化跨引用**：应明确引用：
+   - `gitflow-autoreport-bug`（下游依赖的 skill）
+   - `gitflow-release`（pre-release 冒烟测试是关联场景）
+   - `gitflow-quality`（质量检查与回归测试互补）
+   - `gitflow-pipeline-analyzer`（CI 失败时也可触发回归分析）
+   - `superpowers:systematic-debugging`（失败分类后的调试方法论）
+
+3. **内容组织偏向"操作手册"而非"执行指令"**：
+   - 步骤 1 "确定测试参数"给出一个 bash 块设置变量——这在人类手册中有用，但 Claude 不需要"设置变量"伪代码，而是需要命令行选项速查
+   - 步骤 2 "调用冒烟测试脚本"直接复制步骤 1 的 shell 块——与其让文档这么凝散，不如直接给 3 行命令
+   - 建议：skill 用"命令速查 + 退出码处理 + 失败分类 + 上报决策"的决策式结构
+
+4. **缺少"决策点"分支提示**：
+   - 决策 1：认证是否可达？（no → 提示用户先 `gitflow auth login`）
+   - 决策 2：用户是否明确请求 `--write`？（no → 默认 `--read-only`）
+   - 决策 3：失败是临时性（网络/认证）还是 CLI bug？（temporary → 不调用 autoreport-bug）
+   - 三个决策点应使用 if/else 分支结构
+
+5. **技术命令的质量优势**：
+   - 平台覆盖全面：github/gitlab/gitcode 三个平台都有明确选项
+   - 失败分类体系完整（6 个失败类型 + 严重程度标签）
+   - 体现了作者对"flaky test"场景的意识（注意事项中提到"多次相同失败应优先排查"）
+   - 这些优质内容是重构时应保留的核心内容
+
+### 5.3 评分：⚠️ 需改进
+
+---
+
+## 六、改进建议
+
+### P0（必须修复 — 阻断性问题）
+
+| # | 改进项 | 维度 | 说明 |
+|---|--------|------|------|
+| P0-1 | 重写 description 为触发条件 | D1, D4 | description 必须改为 "Use when..." 格式，仅包含触发关键词 |
+| P0-2 | 添加职责边界声明章节 | D2 | 明确写入模式需用户确认；认证缺失时拒绝运行；临时性失败不上报 |
+| P0-3 | 添加红旗列表 | D2 | 标识敏感场景：认证失效仍运行测试、CI 环境中调用 autoreport-bug、写入模式在非受控环境运行、flaky 失败重复上报 |
+| P0-4 | 添加禁止行为清单 | D2 | 🚫 不得在认证缺失时运行；🚫 不得将认证/网络问题上报为 CLI bug；🚫 未经确认运行写入模式；🚫 不得重复为已知 flaky 失败创建 Issue |
+| P0-5 | 降低 token 数至 < 500 | D1 | 将完整 bash 示例和失败分类 DRY 摘要化；前置检查浓缩为 3 行速查 |
+
+### P1（建议修复 — 提升质量）
+
+| # | 改进项 | 维度 | 说明 |
+|---|--------|------|------|
+| P1-1 | 重构为结构化模板 | D1 | 添加 Overview / When to Use / Core Pattern / Quick Reference / Implementation / Common Mistakes 章节 |
+| P1-2 | 添加 Quick Reference 速查 | D1 | 3 行核心命令（read-only / write / version）+ 退出码处理决策 + 失败上报决策 |
+| P1-3 | 添加决策点分支结构 | D1, D4 | 认证检查 → 写模式确认 → 失败分类 → 上报/抑制 使用明确 if/else 分支 |
+| P1-4 | 添加关键词覆盖 | D4 | 覆盖中英触发词："跑回归测试"、"smoke test"、"冒烟"、"回归"、"pre-release check"、"regression test"、"verify CLI works"、"end-to-end check" |
+| P1-5 | 添加结构化跨引用 | D4 | 引用 gitflow-autoreport-bug（下游）、gitflow-release（pre-release 场景）、gitflow-quality、gitflow-pipeline-analyzer |
+| P1-6 | 添加基线测试场景 | D3 | 定义不用 skill 时 Claude 的基线行为（可能只运行脚本不解析输出、不生成结构化报告） |
+| P1-7 | 补充成功标准 | D3 | 成功报告的最低字段（PASS 数、FAIL 数、SKIP 数、一句话结论）；上报前的验证标志 |
+
+### P2（可选改进 — 锦上添花）
+
+| # | 改进项 | 维度 | 说明 |
+|---|--------|------|------|
+| P2-1 | 添加压力测试场景 | D3 | 全 CLI 损坏场景（避免重复 Issue）、所有测试跳过的场景、脚本损坏的优雅退出、无效平台参数的错误路径 |
+| P2-2 | 提供英文版 description | D1 | Superpowers 主流语言为英文，description 改用英文 |
+| P2-3 | 添加 TDD for skills 验证记录 | D3, D4 | 记录 baseline → 编写 → 验证迭代的过程 |
+| P2-4 | 添加 workflow 流程图 | D4 | 5 步骤 + 2 个决策点用 Mermaid flowchart 简化阅读 |
+| P2-5 | "flaky failure" 豁免机制 | D2, D4 | 引入 `.cache/known-flaky.json`，标记已知 flaky 测试避免重复上报 |
+
+---
+
+## 七、验收标准
+
+- [ ] description 以 "Use when..." 开头，仅描述触发条件（推荐英文）
+- [ ] 含职责边界声明章节（含 🚫 禁止行为、✅/❌ 职责范围）
+- [ ] 含红旗列表（认证缺失、CI 中调用 autoreport-bug、写入模式在非受控环境、flaky 重复上报等）
+- [ ] 含关键词覆盖（中英触发词、工具名、错误信息）
+- [ ] 含跨引用（至少引用 gitflow-autoreport-bug、gitflow-release、gitflow-quality）
+- [ ] 文档结构包含 Overview / When to Use / Core Pattern / Quick Reference / Common Mistakes
+- [ ] 快速加载时 token 数 < 500 词
+- [ ] 含决策点分支结构（认证检查、写模式确认、失败分类）
+- [ ] 含成功标准定义（报告最低字段、上报前验证标志）
+- [ ] 含前置条件检查（凭据可达、脚本存在、平台参数有效性）
+- [ ] 不修改测试脚本、不为用户安装依赖、不重复上报已知 flaky 失败
+
+---
+
+## 八、与同类 Skill 对比
+
+| 对比项 | gitflow-regression | gitflow-autoreport-bug | gitflow-precommit |
+|--------|-------------------|----------------------|-------------------|
+| 边界风险等级 | 🟡 中（间接触发 Issue 创建、写入模式副作用） | 🟡 中（涉及 Issue 创建） | 🟡 中（涉及文件写入） |
+| 职责边界 | ❌ 缺失 | ✅ 完整 | ❌ 缺失 |
+| 测试场景 | ❌ 缺失 | ❌ 缺失 | ❌ 缺失 |
+| 触发条件 | ❌ 功能+流程描述 | ⚠️ 描述流程 | ❌ 功能+流程描述 |
+| 跨引用 | ❌ 仅文字提及 autoreport-bug | ❌ 缺失 | ❌ 缺失 |
+| 命令质量 | ⚠️ 中（脚本平台选项实用，但示例偏重"展示"） | ⚠️ 中 | ✅ 高（多 flag 精确） |
+| description 位置 | ⚠️ 混合功能+流程 | ⚠️ 描述流程 | ⚠️ 混合功能+流程 |
+| Token 数 | ⚠️ 高（885 词） | ⚠️ 中 | ⚠️ 高（885 词） |
+| 结构化程度 | ⚠️ 线性步骤完整 | ✅ 结构化最佳 | ⚠️ 线性步骤完整 |
+| 下游依赖 | ✅ 调用 autoreport-bug（间接边界复杂） | — | — |
+
+**关键发现：** gitflow-regression 是唯一间接触发 another skill（autoreport-bug）的 clone skill——这带来链式边界风险：用户启动回归测试后，若无认证，Claude 仍会尝试 smoke-test→解析失败→触发 autoreport-bug 创建 Issue。这种跨 skill 副作用链需要在两个 skill 的文档中都明确声明。
+
+---
+
+## 九、总结
+
+gitflow-regression 当前的定位是"操作手册 + shell 模板库"混合体——它有完整的工作流步骤和 5 个完整场景示例，但缺乏 Superpowers skill 所需的结构性要素。
+
+核心差距：
+1. **缺乏触发条件** → Claude 何时应加载此 skill？（用户说"跑一下 smoke"？"回归了吗"？"改动后功能正常吗"？）
+2. **缺乏职责边界** → 涉及间接触发 Issue 创建（通过 autoreport-bug）和写入模式（远端资源副作用），边界缺失风险高于纯 read-only skill
+3. **缺乏可测试性** → 如何验证 Claude 在认证检查决策、写模式确认决策、失败分类决策的边界内执行？
+4. **token 超标** → 885 词中包含大量 bash 示例和详细的失败分类 JSON 结构
+
+重构方向：将工作流步骤浓缩为 Quick Reference（3 行命令 + 3 个决策分支），保留失败分类的 DQA 级别描述但移至独立引用文档，添加职责边界声明和红旗列表，重写 description 为触发条件，添加跨引用和 Success Criteria。重构后预期 token 从 885 词降至 ~280 词，大幅提升加载效率。
+
+**特殊关注点（链式边界）：** gitflow-regression 在调用 autoreport-bug 时需要明确：认证和网络问题导致的失败不自动上报；写入模式需要用户确认；flaky 失败识别和抑制机制。这种"skill 调用 skill"的链式边界是所有 skill 中最需要声明清楚的场景。
