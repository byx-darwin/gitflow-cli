# PR #297 Review — fix(pipeline): exclude non-terminal runs from report total_runs

- **PR**: https://github.com/byx-darwin/gitflow-cli/pull/297
- **Branch**: `feat/285-pipeline-report-in-progress-run` → `dev`
- **Closes**: #285
- **Skill used**: `gf-review` (submission-only; requires prior `/gf-pr-review` analysis or explicit verdict)
- **Reviewer**: automated agent run via `gf review`

## Outcome: BLOCKED — no verdict submitted

`gf pr view 297` shows the PR is already `state: "closed"`, `draft: false`, with `mergedAt: "2026-09-02T09:25:06Z"` — i.e. **PR #297 was already merged** before this review run started.

Attempting to submit a review anyway failed as expected:

```
$ gf review approve 297 --body "test"
× Failed to approve PR #297: GitHub CLI 执行失败
📖 文档：https://cli.github.com/manual/
```

GitHub does not allow submitting a new formal review (approve / request-changes / comment-as-review) against a merged pull request. Per the `gf-review` skill's own error-handling table:

> PR not found / closed → Stop. Check number.

No verdict (approve / request-changes / comment) was submitted, and none will be, because the PR is no longer open. This is a process/timing issue, not a finding against the code itself — the review request arrived after the PR had already been merged (it appears to have been merged essentially immediately after creation, `createdAt` and `mergedAt` are ~12 seconds apart).

## What was verified before stopping

- `gf` CLI is authenticated (`gf auth status` → `loggedIn: true`, user `byx-darwin`, scopes include `repo`).
- PR metadata confirms scope matches the task description: GitHub (`crates/github/src/pipeline.rs::report`) and GitLab (`crates/gitlab/src/pipeline.rs::report`) `total_runs` narrowing to terminal-only runs, PR body documents regression tests for both providers (in-progress/success/failure), full workspace test/clippy/fmt reported green in the PR's own test plan, `PipelineStatusEnum` untouched, GitCode explicitly out of scope.
- No independent line-level code review of the diff was performed, since (a) the `gf-review` skill is submission-only and does not itself analyze code, and (b) the PR is already merged, so no review artifact can attach to it on GitHub regardless of analysis outcome.

## Recommendation

If a post-merge quality check of this change is still wanted, use `/gf-pr-review` or `/gf-pr-inline-review` against the merged commit/diff directly (or the range on `dev`) for informational analysis — that would produce findings in chat/report form only, since GitHub will not accept a formal review verdict on a closed PR. If the intent was to review *before* merge, note for future runs that PR #297 merged automatically (or was merged manually) within seconds of creation, leaving no window for a pre-merge review; check branch-protection / auto-merge settings on `dev` if pre-merge review is required going forward.
