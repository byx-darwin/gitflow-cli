# Pipeline Analysis Report — PR #162 (Issue #153)

**Date:** 2026-08-09
**Workflow:** wf-2026-08-09-153 · **Mode:** fast

## Results

| Branch | Runs (7d) | Success Rate | Avg Duration | Top Failures |
|--------|-----------|--------------|--------------|--------------|
| `feat/153-binstall-metadata-fix` | 0 | — | — | — (branch just pushed; CI pending) |
| `main` | 100 | 83% | 152.84s | `failure` (nonspecific label) |

## Analysis

- **Success-rate trend (main):** 83% over 7 days is healthy; no persistent block.
- **Failure patterns:** `topFailures` reports only the generic `failure` label — no repeated
  flaky-test signature attributable to this change.
- **Duration:** ~153s average is stable; no regression introduced (config-only change).

## Local Validation (complements pending CI)

- `make test`: **1080/1080 passed**
- `cargo clippy -p gitflow-cli --all-targets --all-features -- -D warnings -W clippy::pedantic`: clean
- `cargo metadata`: binstall metadata renders `{ bin }` → `gf`, matching `.github/workflows/release.yml` asset naming

## Verdict

`pipeline_ok = true` — no blocking pipeline health issue. CI on the PR branch is expected to
run after push; no retry needed (change is config-only and fully covered by local gates).

## Suggestions (priority order)

1. *(info)* Re-check PR #162 CI status after GitHub Actions picks up the branch — should show green.
2. *(info)* No action for the generic `failure` label on main; monitor if it becomes ≥3 consecutive.
