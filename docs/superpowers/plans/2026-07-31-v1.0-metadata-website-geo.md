# v1.0 元数据 + README 重构 + 官网骨架 + GEO 地基 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 gf 1.0 发布准备代码类交付物：crates.io 元数据中文化、README 重构、Astro 官网骨架 + GitHub Pages、GEO 地基文件（llms.txt / llms-full.txt / JSON-LD / sitemap / robots），全部在单一 PR 内闭环。

**Architecture:** 元数据集中在 Cargo 工作区继承，各 crate 补齐中文主导描述与 docs.rs 链接；官网为 `website/` 下独立 Astro 静态站点（中文主导、原生 CSS、构建期读取仓库内 `docs/compatibility-matrix.json`），由 GitHub Actions 部署到项目站；GEO 文件为 checked-in 静态产物 + 布局内 JSON-LD；Rust 侧以四个"守护测试"在 CI 中长期看护占位符回归、实体一致性、GEO 有效性与演示资产。

**Tech Stack:** Rust 2024（Cargo 工作区、`toml`/`serde_json` 解析做守护测试）、Astro 5（`@astrojs/sitemap` + `@astrojs/mdx`）、GitHub Actions Pages、asciinema + svg-term（演示 SVG 生成，仅开发期）。

## Global Constraints

- **规范一句话定位（全渠道逐字一致，不得改写）**：`跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。`
- **作者**：`byx-darwin <baoyx19870908@gmail.com>`
- **官网 URL**：`https://byx-darwin.github.io/gitflow-cli`
- **本次不改版本号**：保持 `0.9.0`；版本升级 + `cargo publish` 属后续 Issue #97-B，不在本计划。
- **Rust 规范**：Rust 2024 edition、`forbid(unsafe_code)`、生产代码禁 `unwrap()`/`expect()`（测试文件可带 `#![allow(..., reason = "...")]`）、`missing_docs` 警告、通过 `cargo clippy --all-targets --all-features -- -D warnings -W clippy::pedantic`。
- **禁占位符**：任何文件不得出现 `Your Name` / `yourdomain` / `TODO` / `{{version}}` 等模板残留。
- **测试命名**：`test_should_<expected_behavior>`；单测与源码同文件 `#[cfg(test)] mod tests`，跨工件守护测试放 `apps/cli/tests/`。
- **提交规范**：conventional commits（`feat:` / `docs:` / `chore:` / `test:`）。

---

## File Structure

| 文件 | 动作 | 责任 |
|------|------|------|
| `Cargo.toml` | Modify | `[workspace.package]`：改 authors、homepage；删除通用 documentation |
| `apps/cli/Cargo.toml` | Modify | 中文 description、per-crate documentation、keywords |
| `crates/core/Cargo.toml` | Modify | 同上 |
| `crates/github/Cargo.toml` | Modify | 补 authors.workspace / homepage.workspace、中文 description、documentation |
| `crates/gitlab/Cargo.toml` | Modify | 同上 |
| `crates/gitcode/Cargo.toml` | Modify | 同上 |
| `apps/cli/tests/metadata_consistency_test.rs` | Create | 四个守护测试：占位符 / GEO 有效性 / demo 资产 / 实体一致性 + JSON-LD |
| `website/package.json` | Create | Astro 依赖与脚本 |
| `website/astro.config.mjs` | Create | site / base / sitemap / mdx |
| `website/tsconfig.json` | Create | 继承 astro base |
| `website/src/layouts/Base.astro` | Create | HTML 骨架 + 内联 JSON-LD（SoftwareApplication）+ meta/canonical |
| `website/src/styles/global.css` | Create | 原生 CSS 明暗双色 |
| `website/src/pages/index.astro` | Create | 首页 hero（定位 + demo.svg + 安装） |
| `website/src/pages/docs.astro` | Create | 文档索引 |
| `website/src/pages/quickstart.mdx` | Create | 5 分钟快速上手 |
| `website/src/pages/compatibility.astro` | Create | 构建期渲染 compatibility-matrix.json |
| `website/src/pages/changelog.astro` | Create | 渲染 CHANGELOG.md |
| `website/public/llms.txt` | Create | GEO 摘要 |
| `website/public/llms-full.txt` | Create | GEO 全文 |
| `website/public/robots.txt` | Create | 放行 AI 爬虫 + Sitemap |
| `website/public/favicon.svg` | Create | 站点图标 |
| `website/public/demo.svg` | Create | 演示（从 docs/assets 拷贝） |
| `docs/assets/demo.svg` | Create | 演示 SVG 源资产 |
| `scripts/gen-demo-svg.sh` | Create | 演示 SVG 生成脚本（asciinema + svg-term） |
| `scripts/demo-session.sh` | Create | 被录制的只读演示会话 |
| `README.md` | Modify | 中文主导重构：定位 + demo + 安装 + 保留矩阵 |
| `.github/workflows/website.yml` | Create | PR 构建校验 / push main 部署 Pages |

---

## Task 1: crates.io 元数据中文化 + 占位符守护测试

**Files:**
- Modify: `Cargo.toml`（`[workspace.package]` 块）
- Modify: `apps/cli/Cargo.toml`, `crates/core/Cargo.toml`, `crates/github/Cargo.toml`, `crates/gitlab/Cargo.toml`, `crates/gitcode/Cargo.toml`
- Test: `apps/cli/tests/metadata_consistency_test.rs`（Create）

**Interfaces:**
- Produces: 六个 Cargo.toml 的元数据；`workspace_root()` / `read()` / `manifest_paths()` 辅助函数（供后续守护测试任务复用，签名见下）。

- [ ] **Step 1: 写失败测试（RED）**

Create `apps/cli/tests/metadata_consistency_test.rs`：

```rust
//! 跨工件守护测试：元数据卫生、GEO/实体一致性、演示资产。
//!
//! 这些测试在 CI 中长期看护 1.0 发布物的一致性，防止模板占位符回归。
#![allow(
    clippy::expect_used,
    clippy::unwrap_used,
    reason = "守护测试读取已知存在的仓库内工件文件"
)]

use std::fs;
use std::path::PathBuf;

/// 全渠道逐字一致的规范一句话定位。
const CANONICAL_POSITIONING: &str = "跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。";

/// 解析仓库根目录（`apps/cli` 的上两级）。
fn workspace_root() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("../..")
}

/// 读取仓库根下相对路径文件的 UTF-8 文本。
fn read(rel: &str) -> String {
    fs::read_to_string(workspace_root().join(rel))
        .unwrap_or_else(|e| panic!("failed to read {rel}: {e}"))
}

/// 需要检查的全部 Cargo 清单相对路径。
fn manifest_paths() -> Vec<&'static str> {
    vec![
        "Cargo.toml",
        "apps/cli/Cargo.toml",
        "crates/core/Cargo.toml",
        "crates/github/Cargo.toml",
        "crates/gitlab/Cargo.toml",
        "crates/gitcode/Cargo.toml",
    ]
}

#[test]
fn test_should_not_contain_template_placeholders() {
    for rel in manifest_paths() {
        let raw = read(rel);
        assert!(!raw.contains("Your Name"), "{rel} still contains 'Your Name' placeholder");
        assert!(!raw.contains("yourdomain"), "{rel} still contains 'yourdomain' placeholder");
        assert!(!raw.contains("{{version}}"), "{rel} contains unrendered template variable");

        let doc: toml::Value = raw.parse().unwrap_or_else(|e| panic!("{rel} is not valid TOML: {e}"));

        if rel == "Cargo.toml" {
            let pkg = &doc["workspace"]["package"];
            let authors = pkg["authors"].as_array().expect("workspace authors must be an array");
            assert!(!authors.is_empty(), "workspace authors empty");
            let homepage = pkg["homepage"].as_str().expect("workspace homepage missing");
            assert!(homepage.starts_with("https://"), "workspace homepage must be https URL");
            assert!(!homepage.contains("yourdomain"), "workspace homepage has placeholder");
        } else {
            let pkg = &doc["package"];
            let desc = pkg["description"].as_str().unwrap_or_else(|| panic!("{rel}: description missing"));
            assert!(!desc.trim().is_empty(), "{rel}: description empty");
            if let Some(d) = pkg["documentation"].as_str() {
                assert!(
                    d.starts_with("https://docs.rs/"),
                    "{rel}: documentation must point to docs.rs, got {d}"
                );
            }
        }
    }
}
```

- [ ] **Step 2: 运行测试确认失败（RED）**

Run: `cargo test -p gf --test metadata_consistency_test -- --nocapture`
Expected: FAIL —— `Cargo.toml still contains 'Your Name' placeholder`

- [ ] **Step 3: 修改工作区根清单**

Modify `Cargo.toml`，将 `[workspace.package]` 替换为：

```toml
[workspace.package]
version = "0.9.0"
authors = ["byx-darwin <baoyx19870908@gmail.com>"]
edition = "2024"
rust-version = "1.96.0"
license = "MIT"
repository = "https://github.com/byx-darwin/gitflow-cli"
homepage = "https://byx-darwin.github.io/gitflow-cli"
readme = "README.md"
```

（删除原有的 `documentation = "https://docs.rs/"` 行——documentation 改为各 crate 独立设置。）

- [ ] **Step 4: 修改 `apps/cli/Cargo.toml` 的 `[package]`**

将该文件中 `[package]` 的对应字段改为（其余字段与 dependencies 保持不变）：

```toml
[package]
name = "gf"
description = "跨平台 Git 工程化工作流编排框架 CLI：统一封装 GitHub / GitLab / GitCode，配合 AI Agent Skills 覆盖需求到发布的完整工程循环。Cross-platform Git engineering workflow CLI."
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
readme = "README.md"
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/gf"
categories = ["development-tools", "command-line-utilities"]
keywords = ["gitflow", "git", "cli", "workflow", "工作流"]
```

（注意：`documentation.workspace = true` 改为字面量 `documentation = "https://docs.rs/gf"`。）

- [ ] **Step 5: 修改 `crates/core/Cargo.toml` 的 `[package]`**

```toml
[package]
name = "gf-core"
description = "gf 核心库：Platform trait 抽象、跨平台适配器与 toon 输出。Core library: platform abstraction, adapters & toon output."
categories = ["development-tools"]
keywords = ["gitflow", "git", "cli", "core", "workflow"]
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
documentation = "https://docs.rs/gf-core"
repository.workspace = true
homepage.workspace = true
readme = "README.md"
```

- [ ] **Step 6: 修改 `crates/github/Cargo.toml` 的 `[package]`**

```toml
[package]
name = "gf-github"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "gf 的 GitHub 适配器，基于 gh CLI 提供 Issue/PR/Release/Review 等能力。GitHub adapter for gf via gh CLI."
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/gf-github"
readme = "README.md"
keywords = ["cli", "github", "gitflow", "issue", "pr"]
categories = ["command-line-utilities", "development-tools"]
```

- [ ] **Step 7: 修改 `crates/gitlab/Cargo.toml` 的 `[package]`**

```toml
[package]
name = "gf-gitlab"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "gf 的 GitLab 适配器，基于 glab CLI 提供 Issue/MR/Release/Review 等能力。GitLab adapter for gf via glab CLI."
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/gf-gitlab"
readme = "README.md"
keywords = ["cli", "gitlab", "gitflow", "issue", "merge-request"]
categories = ["command-line-utilities", "development-tools"]
```

- [ ] **Step 8: 修改 `crates/gitcode/Cargo.toml` 的 `[package]`**

```toml
[package]
name = "gf-gitcode"
version.workspace = true
authors.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "gf 的 GitCode 适配器，基于 gitcode CLI 提供 Issue/MR/Release/Review 等能力。GitCode adapter for gf via gitcode CLI."
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/gf-gitcode"
readme = "README.md"
keywords = ["cli", "gitcode", "gitflow", "issue", "pr"]
categories = ["command-line-utilities", "development-tools"]
```

- [ ] **Step 9: 运行测试确认通过（GREEN）**

Run: `cargo test -p gf --test metadata_consistency_test -- --nocapture`
Expected: PASS（`test_should_not_contain_template_placeholders` 通过）

- [ ] **Step 10: 构建校验**

Run: `cargo build --workspace 2>&1 | tail -5`
Expected: 编译成功（确认删除 workspace `documentation` 后无 crate 仍引用 `documentation.workspace`）。

- [ ] **Step 11: 提交**

```bash
git add Cargo.toml apps/cli/Cargo.toml crates/core/Cargo.toml crates/github/Cargo.toml crates/gitlab/Cargo.toml crates/gitcode/Cargo.toml apps/cli/tests/metadata_consistency_test.rs
git commit -m "feat(metadata): crates.io 元数据中文化并清除模板占位符 (#97)"
```

---

## Task 2: GEO 地基文件 + 演示 SVG 资产 + 守护测试

**Files:**
- Create: `website/public/llms.txt`, `website/public/llms-full.txt`, `website/public/robots.txt`
- Create: `docs/assets/demo.svg`, `scripts/gen-demo-svg.sh`, `scripts/demo-session.sh`
- Test: `apps/cli/tests/metadata_consistency_test.rs`（追加三个测试）

**Interfaces:**
- Consumes: Task 1 的 `workspace_root()` / `read()` / `CANONICAL_POSITIONING`（同一文件内）。
- Produces: `website/public/llms.txt`（Task 5 实体一致性测试断言其含规范句）、`docs/assets/demo.svg`（Task 3 首页引用、拷贝到 `website/public/demo.svg`）。

- [ ] **Step 1: 写失败测试（RED）**

Append 到 `apps/cli/tests/metadata_consistency_test.rs`：

```rust
#[test]
fn test_should_have_valid_geo_files() {
    let llms = read("website/public/llms.txt");
    assert!(llms.starts_with("# gf"), "llms.txt must start with '# gf'");
    assert!(llms.contains(CANONICAL_POSITIONING), "llms.txt must quote canonical positioning");

    let full = read("website/public/llms-full.txt");
    assert!(full.len() > 500, "llms-full.txt should be a substantial full-text document");
    for section in ["命令", "架构", "兼容性", "FAQ"] {
        assert!(full.contains(section), "llms-full.txt missing section {section}");
    }

    let robots = read("website/public/robots.txt");
    assert!(robots.contains("Sitemap:"), "robots.txt must declare Sitemap");

    let matrix: serde_json::Value =
        serde_json::from_str(&read("docs/compatibility-matrix.json")).expect("compatibility-matrix.json invalid");
    assert_eq!(matrix["schema_version"].as_i64(), Some(1));
    let platforms = matrix["platforms"].as_array().expect("platforms must be an array");
    assert_eq!(platforms.len(), 3, "expected 3 platforms in compatibility matrix");

    let md = read("docs/compatibility-matrix.md");
    for name in ["GitHub", "GitLab", "GitCode"] {
        assert!(md.contains(name), "compatibility-matrix.md missing {name}");
    }
}

#[test]
fn test_should_have_demo_asset() {
    let svg = read("docs/assets/demo.svg");
    assert!(svg.contains("<svg"), "demo.svg must be a valid SVG document");
    assert!(svg.contains("</svg>"), "demo.svg must be closed");
}
```

- [ ] **Step 2: 运行测试确认失败（RED）**

Run: `cargo test -p gf --test metadata_consistency_test -- --nocapture`
Expected: FAIL —— `failed to read website/public/llms.txt`（文件尚不存在）

- [ ] **Step 3: 创建 `website/public/llms.txt`**

```
# gf

> 跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。

gf 是一个 Rust 编写的命令行工具，统一封装 GitHub（gh）、GitLab（glab）、GitCode（gitcode）三大平台的差异，并配套可安装到 Claude Code / Codex / OpenCode / Gemini / Copilot 的 Skills 集合，提供从需求澄清到代码发布的完整 AI 编程工程循环。

## Docs

- [文档索引](https://byx-darwin.github.io/gitflow-cli/docs/): 文档与指南入口
- [5 分钟快速上手](https://byx-darwin.github.io/gitflow-cli/quickstart/): 安装、装 Skills、验证、首次工作流
- [兼容性矩阵](https://byx-darwin.github.io/gitflow-cli/compatibility/): 三平台底层 CLI 版本与功能覆盖
- [更新日志](https://byx-darwin.github.io/gitflow-cli/changelog/): 版本更新历史

## Distribution

- [crates.io](https://crates.io/crates/gf): cargo install gf
- [GitHub](https://github.com/byx-darwin/gitflow-cli): 源码、Issues、Releases
- [Homebrew](https://github.com/byx-darwin/gitflow-cli/tree/main/HomebrewFormula): brew tap byx-darwin/gitflow-cli

## Optional

- [完整材料](https://byx-darwin.github.io/gitflow-cli/llms-full.txt): 完整命令参考 + 架构 + 兼容性 + FAQ
```

- [ ] **Step 4: 创建 `website/public/llms-full.txt`**

```
# gf — 完整材料（llms-full.txt）

> 跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。

本文件面向 AI 大模型与搜索引擎，提供 gf 的完整可引用材料。

## 定位

gf 不是又一个 gh 封装，而是“AI 编程工程循环编排器”：统一封装 GitHub / GitLab / GitCode 三大平台差异（底层分别调用 gh / glab / gitcode CLI），配合可安装到多个 Agent 平台的 Skills 集合，覆盖从需求澄清到代码发布的完整工程循环。

## 安装

Homebrew（macOS）：
  brew tap byx-darwin/gitflow-cli
  brew install gf

Cargo：
  cargo install gf

安装 Skills（项目级，推荐）：
  gf skills install

## 命令参考

- `gf issue {create,list,view,close,reopen,comment}` — Issue 管理
- `gf pr {create,list,view,close,merge,checkout}` — PR 管理
- `gf release {create,list,view,edit}` — 发布管理
- `gf review {comment,approve,request-changes,submit}` — 代码审查
- `gf auth {login,logout,status,token}` — 认证管理
- `gf pipeline {status,logs,jobs,report}` — CI/CD 流水线
- `gf commit {view,diff,patch,comment}` — 提交操作
- `gf label` / `gf milestone` — 标签 / 里程碑管理
- `gf repo {clone,list,create,stats,sync,view}` — 仓库操作
- `gf skills {install,list,uninstall}` — Skills 管理
- `gf completions {bash,zsh,fish}` — Shell 补全

全局标志：`--platform github|gitlab|gitcode`（不指定则基于 git remote 自动检测）、`--output json|text|toon|auto`。

## 架构

- `gf-core`：Platform trait 抽象、跨平台适配器契约、toon 输出。
- `gf-github` / `-gitlab` / `-gitcode`：三平台适配器，分别基于 gh / glab / gitcode CLI。
- `gf`（CLI 应用）：命令路由、输出格式化、Skills 安装。
- 依赖单向流动：应用 → 库；`forbid(unsafe_code)`、pedantic clippy。

## 兼容性

数据源：docs/compatibility-matrix.json（官网兼容性矩阵页构建期渲染）。
- GitHub：gh ≥ 2.0.0
- GitLab：glab ≥ 1.30.0
- GitCode：gitcode ≥ 0.6.0

## FAQ

- gf 和 gh 有什么区别？gh 只服务 GitHub 单一平台；gf 用统一命令面封装 GitHub / GitLab / GitCode 三平台，并叠加面向 AI Agent 的 Skills 工作流。
- 如何给 AI 编程加工程纪律？安装 Skills 后，用 gf-workflow 四阶段编排（需求澄清 → 计划 → 执行 → 交付检查），每阶段有质量门禁。
- 支持哪些 AI Agent 平台？Claude Code / Codex / OpenCode / Gemini CLI / Copilot CLI。

## 许可

MIT。作者：byx-darwin。
```

- [ ] **Step 5: 创建 `website/public/robots.txt`**

```
User-agent: *
Allow: /

User-agent: GPTBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Google-Extended
Allow: /

Sitemap: https://byx-darwin.github.io/gitflow-cli/sitemap-index.xml
# llms.txt: https://byx-darwin.github.io/gitflow-cli/llms.txt
```

- [ ] **Step 6: 创建演示 SVG 资产 `docs/assets/demo.svg`**

先尝试真实生成（Step 8 的脚本）。若执行环境无 asciinema/svg-term，则提交如下静态终端风格 SVG（合法 SVG，非占位符）。创建 `docs/assets/demo.svg`：

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="720" height="300" viewBox="0 0 720 300" font-family="Menlo, Consolas, monospace" font-size="14">
  <rect width="720" height="300" rx="10" fill="#0d1117"/>
  <rect width="720" height="34" rx="10" fill="#161b22"/>
  <circle cx="20" cy="17" r="6" fill="#ff5f56"/><circle cx="40" cy="17" r="6" fill="#ffbd2e"/><circle cx="60" cy="17" r="6" fill="#27c93f"/>
  <text x="20" y="66" fill="#7ee787">$ gf skills install</text>
  <text x="20" y="90" fill="#c9d1d9">✔ 已安装 26 个 gitflow-* skills（项目级 .claude/skills/）</text>
  <text x="20" y="122" fill="#7ee787">$ gf issue list --output toon</text>
  <text x="20" y="146" fill="#c9d1d9">platform: github · 3 open issues</text>
  <text x="20" y="178" fill="#7ee787">$ gf --version</text>
  <text x="20" y="202" fill="#c9d1d9">gf 0.9.0</text>
  <text x="20" y="234" fill="#8b949e"># 统一封装 GitHub / GitLab / GitCode，配合 AI Agent Skills</text>
  <text x="20" y="258" fill="#8b949e"># 覆盖从需求到发布的完整工程循环</text>
</svg>
```

- [ ] **Step 7: 创建生成脚本 `scripts/demo-session.sh`**

```bash
#!/usr/bin/env bash
# 被 asciinema 录制的只读演示会话。需先 `cargo build`。
set -euo pipefail
BIN="${GITFLOW_BIN:-./target/debug/gf}"
run() { printf '\n\033[32m$ %s\033[0m\n' "$*"; "$@" || true; sleep 1; }
run "$BIN" --version
run "$BIN" skills list
run "$BIN" issue list --output toon
```

- [ ] **Step 8: 创建生成脚本 `scripts/gen-demo-svg.sh`**

```bash
#!/usr/bin/env bash
# 用 asciinema + svg-term 将真实会话渲染为 docs/assets/demo.svg。
# 依赖：asciinema、svg-term-cli（npm i -g svg-term-cli）。
set -euo pipefail
ROOT="$(git rev-parse --show-toplevel)"
OUT="${ROOT}/docs/assets/demo.svg"
CAST="$(mktemp /tmp/gitflow-demo.XXXXXX.cast)"
mkdir -p "${ROOT}/docs/assets"
cargo build -p gf
GITFLOW_BIN="${ROOT}/target/debug/gf" \
  asciinema rec "$CAST" --command "bash ${ROOT}/scripts/demo-session.sh" --overwrite
svg-term "$CAST" --out "$OUT" --window --width 90 --height 20
rm -f "$CAST"
echo "Wrote ${OUT}"
```

- [ ] **Step 9: 赋可执行权限并（若工具可用）运行生成**

```bash
chmod +x scripts/gen-demo-svg.sh scripts/demo-session.sh
command -v asciinema >/dev/null && command -v svg-term >/dev/null && ./scripts/gen-demo-svg.sh || echo "tooling unavailable — 使用 Step 6 的静态 SVG"
```

- [ ] **Step 10: 运行测试确认通过（GREEN）**

Run: `cargo test -p gf --test metadata_consistency_test -- --nocapture`
Expected: PASS（`test_should_have_valid_geo_files`、`test_should_have_demo_asset` 通过）

- [ ] **Step 11: 提交**

```bash
git add website/public/llms.txt website/public/llms-full.txt website/public/robots.txt docs/assets/demo.svg scripts/gen-demo-svg.sh scripts/demo-session.sh apps/cli/tests/metadata_consistency_test.rs
git commit -m "feat(geo): 添加 GEO 地基文件与演示 SVG 资产及守护测试 (#97)"
```

---

## Task 3: Astro 官网骨架（配置 + 布局 + 页面 + 样式）

**Files:**
- Create: `website/package.json`, `website/astro.config.mjs`, `website/tsconfig.json`
- Create: `website/src/layouts/Base.astro`, `website/src/styles/global.css`
- Create: `website/src/pages/index.astro`, `docs.astro`, `quickstart.mdx`, `compatibility.astro`, `changelog.astro`
- Create: `website/public/favicon.svg`；拷贝 `docs/assets/demo.svg` → `website/public/demo.svg`

**Interfaces:**
- Consumes: `docs/compatibility-matrix.json`（构建期 import）、`CHANGELOG.md`（`?raw` import）、`docs/assets/demo.svg`。
- Produces: `website/src/layouts/Base.astro`（Task 5 实体一致性 + JSON-LD 测试断言其含规范句与合法 JSON-LD）、`astro build` 产物 `website/dist`。

- [ ] **Step 1: 创建 `website/package.json`**

```json
{
  "name": "gf-website",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.1.0"
  }
}
```

- [ ] **Step 2: 创建 `website/astro.config.mjs`**

```js
import { defineConfig } from "astro/config";
import sitemap from "@astrojs/sitemap";
import mdx from "@astrojs/mdx";

// 项目站：https://byx-darwin.github.io/gitflow-cli
export default defineConfig({
  site: "https://byx-darwin.github.io/gitflow-cli",
  base: "/gf",
  integrations: [sitemap(), mdx()],
});
```

- [ ] **Step 3: 创建 `website/tsconfig.json`**

```json
{
  "extends": "astro/tsconfigs/base"
}
```

- [ ] **Step 4: 创建 `website/src/styles/global.css`**

```css
:root {
  --bg: #ffffff;
  --fg: #1f2328;
  --muted: #57606a;
  --accent: #0969da;
  --code-bg: #f6f8fa;
}
@media (prefers-color-scheme: dark) {
  :root {
    --bg: #0d1117;
    --fg: #e6edf3;
    --muted: #8b949e;
    --accent: #58a6ff;
    --code-bg: #161b22;
  }
}
* { box-sizing: border-box; }
body {
  margin: 0;
  background: var(--bg);
  color: var(--fg);
  font-family: -apple-system, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
  line-height: 1.7;
}
main { max-width: 860px; margin: 0 auto; padding: 2rem 1.25rem 4rem; }
h1 { font-size: 2.25rem; margin-bottom: 0.5rem; }
.tagline { font-size: 1.15rem; color: var(--muted); }
a { color: var(--accent); }
.demo { max-width: 100%; border-radius: 10px; margin: 1.5rem 0; }
pre, code { background: var(--code-bg); border-radius: 6px; }
pre { padding: 1rem; overflow-x: auto; }
.install code { display: block; }
table { border-collapse: collapse; width: 100%; margin: 1rem 0; }
th, td { border: 1px solid var(--muted); padding: 0.5rem 0.75rem; text-align: left; }
.cta a { display: inline-block; margin-right: 1rem; font-weight: 600; }
footer { color: var(--muted); margin-top: 3rem; font-size: 0.9rem; }
```

- [ ] **Step 5: 创建 `website/src/layouts/Base.astro`（含内联 JSON-LD）**

```astro
---
import "../styles/global.css";

export interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。",
} = Astro.props;
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={Astro.url.href} />
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <script type="application/ld+json" is:inline>
{
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "gf",
  "description": "跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "macOS, Linux, Windows",
  "url": "https://byx-darwin.github.io/gitflow-cli/",
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
  "sameAs": [
    "https://github.com/byx-darwin/gitflow-cli",
    "https://crates.io/crates/gf"
  ]
}
    </script>
  </head>
  <body>
    <slot />
    <footer>
      <p>gf · MIT License · <a href="https://github.com/byx-darwin/gitflow-cli">GitHub</a> · <a href="https://crates.io/crates/gf">crates.io</a></p>
    </footer>
  </body>
</html>
```

- [ ] **Step 6: 创建 `website/src/pages/index.astro`**

```astro
---
import Base from "../layouts/Base.astro";
---

<Base title="gf — 跨平台 Git 工程化工作流编排框架">
  <main>
    <h1>gf</h1>
    <p class="tagline">
      跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent
      Skills，覆盖从需求到发布的完整工程循环。
    </p>
    <img src={`${import.meta.env.BASE_URL}demo.svg`} alt="gf 演示" class="demo" />
    <pre class="install"><code>brew tap byx-darwin/gitflow-cli && brew install gf
# 或
cargo install gf</code></pre>
    <nav class="cta">
      <a href={`${import.meta.env.BASE_URL}quickstart/`}>5 分钟快速上手</a>
      <a href={`${import.meta.env.BASE_URL}compatibility/`}>兼容性矩阵</a>
      <a href={`${import.meta.env.BASE_URL}docs/`}>文档</a>
      <a href="https://github.com/byx-darwin/gitflow-cli">GitHub</a>
    </nav>
  </main>
</Base>
```

- [ ] **Step 7: 创建 `website/src/pages/compatibility.astro`**

```astro
---
import Base from "../layouts/Base.astro";
import matrix from "../../../docs/compatibility-matrix.json";
---

<Base title="兼容性矩阵 — gf">
  <main>
    <h1>兼容性矩阵</h1>
    <p>数据源：<code>docs/compatibility-matrix.json</code> · 更新时间：{matrix.updated_at} · gf v{matrix.gitflow_cli_version}</p>
    <table>
      <thead>
        <tr><th>平台</th><th>CLI 工具</th><th>最低版本</th><th>已测试版本</th><th>功能覆盖</th></tr>
      </thead>
      <tbody>
        {matrix.platforms.map((p) => (
          <tr>
            <td>{p.name}</td>
            <td><code>{p.cli_binary}</code></td>
            <td>≥ {p.min_version}</td>
            <td>{p.tested_versions.join(", ")}</td>
            <td>{Object.entries(p.features).filter(([, ok]) => ok).map(([k]) => `${k} ✅`).join(" ")}</td>
          </tr>
        ))}
      </tbody>
    </table>
  </main>
</Base>
```

- [ ] **Step 8: 创建 `website/src/pages/changelog.astro`**

```astro
---
import Base from "../layouts/Base.astro";
import changelog from "../../../CHANGELOG.md?raw";
---

<Base title="更新日志 — gf">
  <main>
    <h1>更新日志</h1>
    <pre><code>{changelog}</code></pre>
  </main>
</Base>
```

- [ ] **Step 9: 创建 `website/src/pages/docs.astro`**

```astro
---
import Base from "../layouts/Base.astro";
---

<Base title="文档 — gf">
  <main>
    <h1>文档</h1>
    <p>gf 的文档与指南。骨架阶段先提供入口，完整文档站随增长期建设。</p>
    <ul>
      <li><a href={`${import.meta.env.BASE_URL}quickstart/`}>5 分钟快速上手</a></li>
      <li><a href={`${import.meta.env.BASE_URL}compatibility/`}>兼容性矩阵</a></li>
      <li><a href={`${import.meta.env.BASE_URL}changelog/`}>更新日志</a></li>
      <li><a href="https://github.com/byx-darwin/gitflow-cli/tree/main/docs">仓库内 docs/ 目录</a></li>
    </ul>
  </main>
</Base>
```

- [ ] **Step 10: 创建 `website/src/pages/quickstart.mdx`**

```mdx
---
layout: ../layouts/Base.astro
title: 5 分钟快速上手 — gf
---

# 5 分钟快速上手

## 1. 安装 gf

```bash
brew tap byx-darwin/gitflow-cli && brew install gf
# 或
cargo install gf
```

## 2. 安装 Skills

```bash
gf skills install        # 项目级（推荐，跟随仓库）
gf skills install -g     # 全局（所有项目可用）
```

## 3. 验证

```bash
gf skills list           # 应看到 26 个 gitflow-* skills
gf --version
```

## 4. 开始开发

在支持的 Agent 平台中输入：

```
/gf-workflow 我要做 X
```

即可进入四阶段编排：需求澄清 → 计划制定 → 执行 → 交付后检查。
```

- [ ] **Step 11: 创建 `website/public/favicon.svg` 并拷贝 demo**

`website/public/favicon.svg`：

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect width="32" height="32" rx="6" fill="#0d1117"/><text x="16" y="22" font-size="18" text-anchor="middle" fill="#7ee787" font-family="monospace">g</text></svg>
```

拷贝演示资产：

```bash
cp docs/assets/demo.svg website/public/demo.svg
```

- [ ] **Step 12: 安装依赖并构建（验证）**

```bash
cd website && npm ci && npm run build && cd ..
```

Expected: 构建成功，生成 `website/dist/`，含 `index.html`、`llms.txt`、`robots.txt`、`sitemap-index.xml`。

- [ ] **Step 13: 提交**

```bash
git add website/
git commit -m "feat(website): 添加 Astro 官网骨架与 GitHub Pages 页面 (#97)"
```

---

## Task 4: README 重构（中文主导）

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: `docs/assets/demo.svg`（引用）、规范一句话定位。
- Produces: README 头部含规范句（Task 5 实体一致性测试断言）。

- [ ] **Step 1: 用以下完整内容替换 `README.md`**

````markdown
# gf

[![CI](https://img.shields.io/github/actions/workflow/status/byx-darwin/gitflow-cli/ci.yml?branch=main)](https://github.com/byx-darwin/gitflow-cli/actions)
[![Release](https://img.shields.io/github/v/release/byx-darwin/gitflow-cli)](https://github.com/byx-darwin/gitflow-cli/releases/latest)
[![Rust 2024](https://img.shields.io/badge/Rust-2024-orange?logo=rust)](https://www.rust-lang.org/)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue)](LICENSE)

**跨平台 Git 工程化工作流编排框架：统一封装 GitHub / GitLab / GitCode 三大平台，配合 AI Agent Skills，覆盖从需求到发布的完整工程循环。**

![gf 演示](docs/assets/demo.svg)

## 安装

```bash
# Homebrew (macOS)
brew tap byx-darwin/gitflow-cli
brew install gf

# 或 Cargo
cargo install gf
```

## 30 秒上手

```bash
# 1. 安装 Skills（项目级，跟随仓库）
gf skills install

# 2. 验证
gf skills list     # 应看到 26 个 gitflow-* skills
gf --version

# 3. 在 Agent 平台中进入四阶段工作流
/gf-workflow 我要做 X
```

## 平台支持

### Git 平台

`gf` 统一封装三大 Git 平台差异，`--platform` 自动检测或手动指定：

| 平台 | CLI 依赖 | 特性 |
|------|---------|------|
| **GitHub** | `gh` (v2.0.0+) | Issue / PR / Release / Review / Pipeline / Repo（含 Enterprise） |
| **GitLab** | `glab` (v1.30.0+) | Issue / PR(MR) / Release / Review / Pipeline / Repo（含自建实例） |
| **GitCode** | `gitcode` (v0.6.0+) | Issue / PR(MR) / Release / Review / Pipeline / Repo |

```bash
gf issue list                                  # 自动检测（基于 git remote）
gf issue list --platform gitlab --output text  # 手动指定平台
```

详见官网[兼容性矩阵](https://byx-darwin.github.io/gitflow-cli/compatibility/)。

### Agent 平台

Skills 可安装到任意支持的 AI Agent 平台，`--agent` 指定目标（不指定则自动检测）。安装位置分项目级（默认，装到当前仓库 `.claude/` 等）与全局级（`-g`，装到 `~/`）。

| Agent | 项目级目录 | 全局级目录 | Stop Hook 支持 |
|-------|-----------|-----------|----------------|
| **Claude Code** | `.claude/skills/` | `~/.claude/skills/` | ✅ |
| **Codex** (OpenAI) | `.codex/skills/` | `~/.codex/skills/` | ✅ |
| **OpenCode** | `.opencode/skills/` | `~/.opencode/skills/` | ❌ 跳过 |
| **Gemini CLI** | `.gemini/skills/` | `~/.gemini/skills/` | ❌ 跳过 |
| **Copilot CLI** | `.copilot/skills/` | `~/.copilot/skills/` | ❌ 跳过 |

## Skill 矩阵

| 层 | Skill | 做什么 |
|----|-------|--------|
| 编排 | `gf-workflow` | 四阶段全流程编排：需求澄清 → 计划制定 → 执行 → 交付后检查 |
| 编排 | `gf-quality` | 本地质量门禁：build → test → coverage → format → static → pre-commit |
| Issue | `gf-issue-create` / `gf-issue-review` / `gf-issue-triage` | 创建 / 需求审查 / 分类分流 |
| PR | `gf-pr-create` / `gf-pr-review` / `gf-pr-inline-review` / `gf-pr-apply-feedback` | 创建 / 6 维审查 / 逐行评论 / 应用反馈 |
| 交付 | `gf-release-helper` / `gf-label-stats` / `gf-pipeline-analyzer` | Release Note / 标签统计 / 流水线健康 |
| 辅助 | `gf-security-check` / `gf-precommit` / `gf-regression` / `gf-repo-onboarding` / `gf-autoreport-bug` | 安全审计 / 预提交 / 回归 / 入门 / 自动报障 |

## CLI 命令一览

| 命令 | 用途 |
|------|------|
| `gf issue {create,list,view,close,reopen,comment}` | Issue 管理 |
| `gf pr {create,list,view,close,merge,checkout}` | PR 管理 |
| `gf release {create,list,view,edit}` | 发布管理 |
| `gf review {comment,approve,request-changes,submit}` | 代码审查 |
| `gf auth {login,logout,status,token}` | 认证管理 |
| `gf pipeline {status,logs,jobs,report}` | CI/CD 流水线 |
| `gf commit {view,diff,patch,comment}` | 提交操作 |
| `gf label/milestone` | 标签/里程碑管理 |
| `gf repo {clone,list,create,stats,sync,view}` | 仓库操作 |
| `gf skills {install,list,uninstall}` | Skills 管理 |
| `gf completions {bash,zsh,fish}` | Shell 补全 |

支持 `--platform github|gitlab|gitcode` 与 `--output json|text|toon|auto`。

## 文档与官网

官方网站：<https://byx-darwin.github.io/gitflow-cli>

- [5 分钟快速上手](https://byx-darwin.github.io/gitflow-cli/quickstart/)
- [兼容性矩阵](https://byx-darwin.github.io/gitflow-cli/compatibility/)
- [更新日志](https://byx-darwin.github.io/gitflow-cli/changelog/)
- 仓库内文档：[`docs/`](docs/index.md)

## 设计原则

- **步骤化工作流**：每个 skill 有明确步骤顺序，不跳步。
- **先验证再行动**：PR 创建前检查分支与变更；Issue 创建前引导填写模板。
- **生态互补**：本地开发循环（Superpowers）+ 远端协作（gf）明确分工。
- **多 Agent 兼容**：skills 可安装到 Claude Code / Codex / OpenCode / Gemini / Copilot。
- **质量门闸门**：build → test → coverage → format → static → pre-commit 全部通过才能交付。

## 贡献

详见 [CONTRIBUTING.md](CONTRIBUTING.md)。
````

- [ ] **Step 2: 校验 README 引用的资产存在**

Run: `test -f docs/assets/demo.svg && echo "demo.svg OK"`
Expected: `demo.svg OK`

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "docs(readme): 中文主导重构，开头一句话定位 + 演示 + 安装 (#97)"
```

---

## Task 5: 实体一致性 + JSON-LD 守护测试

**Files:**
- Modify: `apps/cli/tests/metadata_consistency_test.rs`（追加两个测试）

**Interfaces:**
- Consumes: README.md（Task 4）、`website/src/layouts/Base.astro`（Task 3）、`website/public/llms.txt`（Task 2）。

- [ ] **Step 1: 写失败测试（RED）**

Append 到 `apps/cli/tests/metadata_consistency_test.rs`：

```rust
#[test]
fn test_should_keep_entity_consistency() {
    let readme = read("README.md");
    assert!(readme.contains(CANONICAL_POSITIONING), "README.md missing canonical positioning");

    let llms = read("website/public/llms.txt");
    assert!(llms.contains(CANONICAL_POSITIONING), "llms.txt missing canonical positioning");

    let base = read("website/src/layouts/Base.astro");
    assert!(base.contains(CANONICAL_POSITIONING), "Base.astro missing canonical positioning");
}

#[test]
fn test_should_have_valid_jsonld() {
    let base = read("website/src/layouts/Base.astro");
    let marker = base.find("application/ld+json").expect("Base.astro missing JSON-LD script");
    let after = &base[marker..];
    let json_start = after.find('>').expect("malformed JSON-LD open tag") + 1;
    let json_end = after.find("</script>").expect("JSON-LD not closed");
    let json_text = &after[json_start..json_end];
    let v: serde_json::Value = serde_json::from_str(json_text).expect("JSON-LD is not valid JSON");

    assert_eq!(v["@type"].as_str(), Some("SoftwareApplication"));
    assert_eq!(v["name"].as_str(), Some("gf"));
    assert!(
        v["description"].as_str().is_some_and(|d| d.contains("跨平台 Git 工程化工作流编排框架")),
        "JSON-LD description missing positioning"
    );
    assert!(v["applicationCategory"].as_str().is_some(), "applicationCategory required");
    assert!(
        v["sameAs"].as_array().is_some_and(|a| !a.is_empty()),
        "sameAs must be a non-empty array"
    );
}
```

- [ ] **Step 2: 运行测试确认通过（已满足则 GREEN）**

Run: `cargo test -p gf --test metadata_consistency_test -- --nocapture`
Expected: PASS（README/Base.astro/llms.txt 均已含规范句，JSON-LD 合法）。
若 FAIL，说明前序任务的文案与规范句不一致——回到对应 Task 修正为逐字一致，再重跑。

- [ ] **Step 3: 全量运行守护测试**

Run: `cargo test -p gf --test metadata_consistency_test -- --nocapture`
Expected: 全部 6 个测试 PASS（占位符 / GEO 有效性 / demo 资产 / 实体一致性 / JSON-LD，以及 Step 1 的两个）。

- [ ] **Step 4: 提交**

```bash
git add apps/cli/tests/metadata_consistency_test.rs
git commit -m "test(metadata): 添加实体一致性与 JSON-LD 守护测试 (#97)"
```

---

## Task 6: GitHub Pages 部署工作流

**Files:**
- Create: `.github/workflows/website.yml`

**Interfaces:**
- Consumes: `website/`（Task 3）。
- Produces: PR 构建校验 + push main 部署到 Pages。

- [ ] **Step 1: 创建 `.github/workflows/website.yml`**

```yaml
name: Website

on:
  push:
    branches: [main]
    paths: ["website/**", ".github/workflows/website.yml"]
  pull_request:
    branches: [main]
    paths: ["website/**", ".github/workflows/website.yml"]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: website-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install
        working-directory: website
        run: npm ci
      - name: Build
        working-directory: website
        run: npm run build
      - name: Upload Pages artifact
        if: github.event_name == 'push'
        uses: actions/upload-pages-artifact@v3
        with:
          path: website/dist

  deploy:
    name: Deploy
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: YAML 语法校验**

Run: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/website.yml')); print('YAML OK')"`
Expected: `YAML OK`

- [ ] **Step 3: 提交**

```bash
git add .github/workflows/website.yml
git commit -m "ci(website): 添加官网 GitHub Pages 构建与部署工作流 (#97)"
```

---

## Task 7: 质量门 + 发布预检 + 文档索引

**Files:**
- Modify: `docs/index.md`（登记新文档/官网链接，若其结构需要）

**Interfaces:**
- Consumes: 前六任务全部产物。

- [ ] **Step 1: Rust 全量质量门**

Run: `make fmt && make clippy 2>&1 | tail -20`
Expected: 无格式变更、clippy 无告警（含 pedantic）。若有告警，修复后重跑。

- [ ] **Step 2: Rust 测试**

Run: `make test 2>&1 | tail -20`
Expected: 全部测试通过（含新增守护测试）。

- [ ] **Step 3: crates.io 发布预检（不实际 publish）**

Run: `cargo package --list -p gf 2>&1 | tail -10`
Expected: 列出将打包的文件，无错误（验证清单字段合法、include 有效）。

- [ ] **Step 4: 站点构建终验**

Run: `cd website && npm ci && npm run build && ls dist && cd ..`
Expected: `dist/` 含 index.html、llms.txt、llms-full.txt、robots.txt、sitemap-index.xml、demo.svg。

- [ ] **Step 5: 更新 `docs/index.md`**

检查 `docs/index.md`，在合适分区补充官网与 GEO 条目（若无对应分区则新增“官网与 GEO”小节）：

```markdown
## 官网与 GEO

- 官方网站：<https://byx-darwin.github.io/gitflow-cli>（源码见 `website/`）
- GEO 地基：`website/public/llms.txt`、`website/public/llms-full.txt`、`website/public/robots.txt`、`website/src/layouts/Base.astro`（JSON-LD）
- 演示资产：`docs/assets/demo.svg`（生成脚本 `scripts/gen-demo-svg.sh`）
- 设计文档：`docs/superpowers/specs/2026-07-31-v1.0-metadata-website-geo-design.md`
```

- [ ] **Step 6: 提交**

```bash
git add docs/index.md
git commit -m "docs(index): 登记官网与 GEO 地基条目 (#97)"
```

- [ ] **Step 7: 终验清单核对**

逐项确认：
- [ ] 六个 Cargo.toml 无 `Your Name`/`yourdomain`，description 中文主导，documentation 指向各自 docs.rs
- [ ] README 头部 = 规范一句话定位 + demo.svg + 安装
- [ ] `website/` 构建成功，含 5 页 + GEO 文件 + sitemap
- [ ] 守护测试 6 个全绿
- [ ] `website.yml` YAML 合法
- [ ] `cargo package --list` 通过
