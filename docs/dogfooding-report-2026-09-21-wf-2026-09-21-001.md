## Dogfooding Summary — v1.9.0 (Phase 4, wf-2026-09-21-001, Issue #366)

**Date:** 2026-09-21
**Executor:** gf-workflow (Claude Code session)
**Result:** PASS

| Platform | Items | Passed | Failed | Notes |
|----------|-------|--------|--------|-------|
| GitHub   | 4     | 4      | 0      | OK — release create/delete, non-interactive mode, delete idempotency all confirmed. Confirmed real-world: GitHub's release API always returns `createdAt` (never omits it), consistent with Issue #366's design assumption that GitHub needed no fallback code. |
| GitLab   | 5     | 5      | 0      | OK — used internal GitLab instance `192.168.230.23` (`iproost/iproost-docs`) since `gitlab.com` had no valid token (401, no fallback attempted — flagged to user, who redirected to the internal instance). Chinese label `测试标签` created/displayed/deleted with zero encoding issues end to end. |
| GitCode  | 4     | 4      | 0      | OK — used `byx-darwin/NexaTrade` test repo. Non-interactive `pr merge` completed with no confirmation prompt (the historical Issue #70 risk did not reproduce). |

**Bugs Found:** 0

**Observations (non-blocking, not filed as bugs):**
- `gitlab.com` has no auth token configured in this environment (401 Unauthorized) — this is an environment/credential gap, not a `gf` defect; GitLab dogfooding was redirected to an authenticated internal instance instead.
- `gf pr create --platform gitcode`'s JSON response has empty `author`/`baseBranch`/`headBranch`/`url` fields even on success — worth a follow-up look if gitcode PR metadata completeness ever becomes load-bearing, but out of scope for #366.
- `gf release delete` on an already-deleted GitHub release returns `{"deleted": true}` rather than a not-found signal — this satisfies the checklist's own idempotency requirement ("repeated delete does not error") but is worth noting as a design choice (swallowing 404 as success) rather than a true state check.

**Release Decision:** APPROVED
